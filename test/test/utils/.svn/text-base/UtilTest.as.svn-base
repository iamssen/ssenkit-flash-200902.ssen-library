package test.utils 
{
	import ssen.core.display.SSenSprite;	
	/**
	 * @author SSen
	 */
	public class UtilTest extends SSenSprite
	{
		public function UtilTest()
		{
			var p : Array = ["a", "b", "c"];
			p.unshift("d");
			
			trace.apply(null, p);
		}
	}
}
